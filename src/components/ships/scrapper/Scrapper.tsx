import React from 'react';

interface ScrapperProps {
  // Props will be added later
}

const Scrapper: React.FC<ScrapperProps> = () => {
  return (
    <div>
      {/* Component content will be added later */}
    </div>
  );
};

export default Scrapper; 