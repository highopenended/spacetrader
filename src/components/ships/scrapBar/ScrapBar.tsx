import React from 'react';

interface ScrapBarProps {
  // Props will be added later
}

const ScrapBar: React.FC<ScrapBarProps> = () => {
  return (
    <div>
      {/* Component content will be added later */}
    </div>
  );
};

export default ScrapBar; 